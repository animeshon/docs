---
id: relation
title: Relation
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type Relation implements Metadata {
  id: String!
  selfLink: String!
  createdAt: DateTime!
  modifiedAt: DateTime!
  version: String!
  subject: WithRelation!
  object: Generic!
  label(filter: RelationLabelFilter): RelationLabel!
  from: DateTime
  to: DateTime
}
```

### Fields

#### `id` ([`String!`](/docs/schema/scalars/string))

#### `selfLink` ([`String!`](/docs/schema/scalars/string))

#### `createdAt` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `modifiedAt` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `version` ([`String!`](/docs/schema/scalars/string))

#### `subject` ([`WithRelation!`](/docs/schema/interfaces/with-relation))

Organization, Person, Anime, Manga, Character

#### `object` ([`Generic!`](/docs/schema/interfaces/generic))

Organization, Person, Anime, Manga, Character

#### `label` ([`RelationLabel!`](/docs/schema/objects/relation-label))

- ##### `filter` ([`RelationLabelFilter`](/docs/schema/inputs/relation-label-filter))

#### `from` ([`DateTime`](/docs/schema/scalars/date-time))

if null it is unknown the status

#### `to` ([`DateTime`](/docs/schema/scalars/date-time))

if 1970 or 0000 it means a generic end of the relation, if null it is unknown the status

### Interfaces

#### [`Metadata`](/docs/schema/interfaces/metadata)
