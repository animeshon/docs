---
id: medium
title: Medium
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type Medium implements Metadata {
  id: String!
  selfLink: String!
  createdAt: DateTime!
  modifiedAt: DateTime!
  version: String!
  type: MediumType!
  quantity: Int!
}
```

### Fields

#### `id` ([`String!`](/docs/schema/scalars/string))

#### `selfLink` ([`String!`](/docs/schema/scalars/string))

#### `createdAt` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `modifiedAt` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `version` ([`String!`](/docs/schema/scalars/string))

#### `type` ([`MediumType!`](/docs/schema/enums/medium-type))

vector of the content

#### `quantity` ([`Int!`](/docs/schema/scalars/int))

amount of vetors used to deliver the content

### Interfaces

#### [`Metadata`](/docs/schema/interfaces/metadata)
