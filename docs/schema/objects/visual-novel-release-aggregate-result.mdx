---
id: visual-novel-release-aggregate-result
title: VisualNovelReleaseAggregateResult
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type VisualNovelReleaseAggregateResult {
  count: Int
}
```

### Fields

#### `count` ([`Int`](/docs/schema/scalars/int))
