---
id: search-suggestion-query-result
title: SearchSuggestionQueryResult
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SearchSuggestionQueryResult {
  results: [SuggestionItem!]!
}
```

### Fields

#### `results` ([`[SuggestionItem!]!`](/docs/schema/objects/suggestion-item))
