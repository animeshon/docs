---
id: broadcast
title: Broadcast
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type Broadcast implements Metadata {
  id: String!
  selfLink: String!
  createdAt: DateTime!
  modifiedAt: DateTime!
  version: String!
  content(filter: EpisodeFilter): Episode!
  broadcaster(filter: OrganizationFilter): Organization!
  from: DateTime!
  to: DateTime!
  localization(filter: LocalizationFilter): Localization!
}
```

### Fields

#### `id` ([`String!`](/docs/schema/scalars/string))

#### `selfLink` ([`String!`](/docs/schema/scalars/string))

#### `createdAt` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `modifiedAt` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `version` ([`String!`](/docs/schema/scalars/string))

#### `content` ([`Episode!`](/docs/schema/objects/episode))

- ##### `filter` ([`EpisodeFilter`](/docs/schema/inputs/episode-filter))

#### `broadcaster` ([`Organization!`](/docs/schema/objects/organization))

- ##### `filter` ([`OrganizationFilter`](/docs/schema/inputs/organization-filter))

#### `from` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `to` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `localization` ([`Localization!`](/docs/schema/objects/localization))

- ##### `filter` ([`LocalizationFilter`](/docs/schema/inputs/localization-filter))

### Interfaces

#### [`Metadata`](/docs/schema/interfaces/metadata)
