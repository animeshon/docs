---
id: search-query-result
title: SearchQueryResult
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SearchQueryResult {
  resultTotal: Int!
  results: [SearchItem!]!
}
```

### Fields

#### `resultTotal` ([`Int!`](/docs/schema/scalars/int))

#### `results` ([`[SearchItem!]!`](/docs/schema/objects/search-item))
