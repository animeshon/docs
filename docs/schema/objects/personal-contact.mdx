---
id: personal-contact
title: PersonalContact
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PersonalContact implements Metadata {
  id: String!
  selfLink: String!
  createdAt: DateTime!
  modifiedAt: DateTime!
  version: String!
  email(filter: EmailAddressFilter): EmailAddress
  phone(filter: PhoneNumberFilter): PhoneNumber
  fax(filter: PhoneNumberFilter): PhoneNumber
}
```

### Fields

#### `id` ([`String!`](/docs/schema/scalars/string))

#### `selfLink` ([`String!`](/docs/schema/scalars/string))

#### `createdAt` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `modifiedAt` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `version` ([`String!`](/docs/schema/scalars/string))

#### `email` ([`EmailAddress`](/docs/schema/objects/email-address))

- ##### `filter` ([`EmailAddressFilter`](/docs/schema/inputs/email-address-filter))

#### `phone` ([`PhoneNumber`](/docs/schema/objects/phone-number))

- ##### `filter` ([`PhoneNumberFilter`](/docs/schema/inputs/phone-number-filter))

#### `fax` ([`PhoneNumber`](/docs/schema/objects/phone-number))

- ##### `filter` ([`PhoneNumberFilter`](/docs/schema/inputs/phone-number-filter))

### Interfaces

#### [`Metadata`](/docs/schema/interfaces/metadata)
