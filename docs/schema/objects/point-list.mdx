---
id: point-list
title: PointList
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PointList {
  points: [Point!]!
}
```

### Fields

#### `points` ([`[Point!]!`](/docs/schema/objects/point))
