---
id: suggestion-item
title: SuggestionItem
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SuggestionItem {
  terms: [LocalizedString!]!
}
```

### Fields

#### `terms` ([`[LocalizedString!]!`](/docs/schema/objects/localized-string))
