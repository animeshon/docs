---
id: anime-aggregate-result
title: AnimeAggregateResult
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type AnimeAggregateResult {
  count: Int
}
```

### Fields

#### `count` ([`Int`](/docs/schema/scalars/int))
