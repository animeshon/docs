---
id: content-relation-type-hash
title: ContentRelationType_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ContentRelationType_hash {
  eq: ContentRelationType!
  in: [ContentRelationType!]
}
```

### Fields

#### `eq` ([`ContentRelationType!`](/docs/schema/enums/content-relation-type))

#### `in` ([`[ContentRelationType!]`](/docs/schema/enums/content-relation-type))
