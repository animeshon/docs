---
id: canonical-filter
title: CanonicalFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CanonicalFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  has: CanonicalHasFilter
  and: [CanonicalFilter]
  or: [CanonicalFilter]
  not: CanonicalFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `has` ([`CanonicalHasFilter`](/docs/schema/enums/canonical-has-filter))

#### `and` ([`[CanonicalFilter]`](/docs/schema/inputs/canonical-filter))

#### `or` ([`[CanonicalFilter]`](/docs/schema/inputs/canonical-filter))

#### `not` ([`CanonicalFilter`](/docs/schema/inputs/canonical-filter))
