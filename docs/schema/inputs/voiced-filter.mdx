---
id: voiced-filter
title: VoicedFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type VoicedFilter {
  has: VoicedHasFilter
  and: [VoicedFilter]
  or: [VoicedFilter]
  not: VoicedFilter
}
```

### Fields

#### `has` ([`VoicedHasFilter`](/docs/schema/enums/voiced-has-filter))

#### `and` ([`[VoicedFilter]`](/docs/schema/inputs/voiced-filter))

#### `or` ([`[VoicedFilter]`](/docs/schema/inputs/voiced-filter))

#### `not` ([`VoicedFilter`](/docs/schema/inputs/voiced-filter))
