---
id: voice-acting-ref
title: VoiceActingRef
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type VoiceActingRef {
  id: String
  selfLink: String
  createdAt: DateTime
  modifiedAt: DateTime
  version: String
  actor: PersonRef
  isPrimary: Boolean
  localization: LocalizationRef
}
```

### Fields

#### `id` ([`String`](/docs/schema/scalars/string))

#### `selfLink` ([`String`](/docs/schema/scalars/string))

#### `createdAt` ([`DateTime`](/docs/schema/scalars/date-time))

#### `modifiedAt` ([`DateTime`](/docs/schema/scalars/date-time))

#### `version` ([`String`](/docs/schema/scalars/string))

#### `actor` ([`PersonRef`](/docs/schema/inputs/person-ref))

#### `isPrimary` ([`Boolean`](/docs/schema/scalars/boolean))

Is the voice for a secondary character?

#### `localization` ([`LocalizationRef`](/docs/schema/inputs/localization-ref))
