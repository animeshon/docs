---
id: image-filter
title: ImageFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ImageFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  has: ImageHasFilter
  and: [ImageFilter]
  or: [ImageFilter]
  not: ImageFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `has` ([`ImageHasFilter`](/docs/schema/enums/image-has-filter))

#### `and` ([`[ImageFilter]`](/docs/schema/inputs/image-filter))

#### `or` ([`[ImageFilter]`](/docs/schema/inputs/image-filter))

#### `not` ([`ImageFilter`](/docs/schema/inputs/image-filter))
