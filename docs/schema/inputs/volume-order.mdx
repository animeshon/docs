---
id: volume-order
title: VolumeOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type VolumeOrder {
  asc: VolumeOrderable
  desc: VolumeOrderable
  then: VolumeOrder
}
```

### Fields

#### `asc` ([`VolumeOrderable`](/docs/schema/enums/volume-orderable))

#### `desc` ([`VolumeOrderable`](/docs/schema/enums/volume-orderable))

#### `then` ([`VolumeOrder`](/docs/schema/inputs/volume-order))
