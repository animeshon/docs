---
id: generate-query-params
title: GenerateQueryParams
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type GenerateQueryParams {
  get: Boolean
  query: Boolean
  password: Boolean
  aggregate: Boolean
}
```

### Fields

#### `get` ([`Boolean`](/docs/schema/scalars/boolean))

#### `query` ([`Boolean`](/docs/schema/scalars/boolean))

#### `password` ([`Boolean`](/docs/schema/scalars/boolean))

#### `aggregate` ([`Boolean`](/docs/schema/scalars/boolean))
