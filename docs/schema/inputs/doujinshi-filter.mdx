---
id: doujinshi-filter
title: DoujinshiFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type DoujinshiFilter {
  status: ContentStatus_hash
  id: StringHashFilter
  selfLink: StringHashFilter
  has: DoujinshiHasFilter
  and: [DoujinshiFilter]
  or: [DoujinshiFilter]
  not: DoujinshiFilter
}
```

### Fields

#### `status` ([`ContentStatus_hash`](/docs/schema/inputs/content-status-hash))

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `has` ([`DoujinshiHasFilter`](/docs/schema/enums/doujinshi-has-filter))

#### `and` ([`[DoujinshiFilter]`](/docs/schema/inputs/doujinshi-filter))

#### `or` ([`[DoujinshiFilter]`](/docs/schema/inputs/doujinshi-filter))

#### `not` ([`DoujinshiFilter`](/docs/schema/inputs/doujinshi-filter))
