---
id: writing-system-hash
title: WritingSystem_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type WritingSystem_hash {
  eq: WritingSystem!
  in: [WritingSystem!]
}
```

### Fields

#### `eq` ([`WritingSystem!`](/docs/schema/enums/writing-system))

#### `in` ([`[WritingSystem!]`](/docs/schema/enums/writing-system))
