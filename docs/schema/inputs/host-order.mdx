---
id: host-order
title: HostOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type HostOrder {
  asc: HostOrderable
  desc: HostOrderable
  then: HostOrder
}
```

### Fields

#### `asc` ([`HostOrderable`](/docs/schema/enums/host-orderable))

#### `desc` ([`HostOrderable`](/docs/schema/enums/host-orderable))

#### `then` ([`HostOrder`](/docs/schema/inputs/host-order))
