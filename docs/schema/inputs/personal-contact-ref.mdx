---
id: personal-contact-ref
title: PersonalContactRef
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PersonalContactRef {
  id: String
  selfLink: String
  createdAt: DateTime
  modifiedAt: DateTime
  version: String
  email: EmailAddressRef
  phone: PhoneNumberRef
  fax: PhoneNumberRef
}
```

### Fields

#### `id` ([`String`](/docs/schema/scalars/string))

#### `selfLink` ([`String`](/docs/schema/scalars/string))

#### `createdAt` ([`DateTime`](/docs/schema/scalars/date-time))

#### `modifiedAt` ([`DateTime`](/docs/schema/scalars/date-time))

#### `version` ([`String`](/docs/schema/scalars/string))

#### `email` ([`EmailAddressRef`](/docs/schema/inputs/email-address-ref))

#### `phone` ([`PhoneNumberRef`](/docs/schema/inputs/phone-number-ref))

#### `fax` ([`PhoneNumberRef`](/docs/schema/inputs/phone-number-ref))
