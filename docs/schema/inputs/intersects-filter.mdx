---
id: intersects-filter
title: IntersectsFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type IntersectsFilter {
  polygon: PolygonRef
  multiPolygon: MultiPolygonRef
}
```

### Fields

#### `polygon` ([`PolygonRef`](/docs/schema/inputs/polygon-ref))

#### `multiPolygon` ([`MultiPolygonRef`](/docs/schema/inputs/multi-polygon-ref))
