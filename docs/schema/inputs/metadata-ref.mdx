---
id: metadata-ref
title: MetadataRef
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type MetadataRef {
  id: String!
}
```

### Fields

#### `id` ([`String!`](/docs/schema/scalars/string))
