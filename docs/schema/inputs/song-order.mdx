---
id: song-order
title: SongOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SongOrder {
  asc: SongOrderable
  desc: SongOrderable
  then: SongOrder
}
```

### Fields

#### `asc` ([`SongOrderable`](/docs/schema/enums/song-orderable))

#### `desc` ([`SongOrderable`](/docs/schema/enums/song-orderable))

#### `then` ([`SongOrder`](/docs/schema/inputs/song-order))
