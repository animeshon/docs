---
id: metadata-order
title: MetadataOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type MetadataOrder {
  asc: MetadataOrderable
  desc: MetadataOrderable
  then: MetadataOrder
}
```

### Fields

#### `asc` ([`MetadataOrderable`](/docs/schema/enums/metadata-orderable))

#### `desc` ([`MetadataOrderable`](/docs/schema/enums/metadata-orderable))

#### `then` ([`MetadataOrder`](/docs/schema/inputs/metadata-order))
