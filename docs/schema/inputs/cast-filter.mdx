---
id: cast-filter
title: CastFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CastFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  relation: CastingType_hash
  has: CastHasFilter
  and: [CastFilter]
  or: [CastFilter]
  not: CastFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `relation` ([`CastingType_hash`](/docs/schema/inputs/casting-type-hash))

#### `has` ([`CastHasFilter`](/docs/schema/enums/cast-has-filter))

#### `and` ([`[CastFilter]`](/docs/schema/inputs/cast-filter))

#### `or` ([`[CastFilter]`](/docs/schema/inputs/cast-filter))

#### `not` ([`CastFilter`](/docs/schema/inputs/cast-filter))
