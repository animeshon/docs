---
id: game-release-type-hash
title: GameReleaseType_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type GameReleaseType_hash {
  eq: GameReleaseType!
  in: [GameReleaseType!]
}
```

### Fields

#### `eq` ([`GameReleaseType!`](/docs/schema/enums/game-release-type))

#### `in` ([`[GameReleaseType!]`](/docs/schema/enums/game-release-type))
