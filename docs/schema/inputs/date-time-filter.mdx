---
id: date-time-filter
title: DateTimeFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type DateTimeFilter {
  eq: DateTime
  le: DateTime
  lt: DateTime
  ge: DateTime
  gt: DateTime
  between: DateTimeRange
}
```

### Fields

#### `eq` ([`DateTime`](/docs/schema/scalars/date-time))

#### `le` ([`DateTime`](/docs/schema/scalars/date-time))

#### `lt` ([`DateTime`](/docs/schema/scalars/date-time))

#### `ge` ([`DateTime`](/docs/schema/scalars/date-time))

#### `gt` ([`DateTime`](/docs/schema/scalars/date-time))

#### `between` ([`DateTimeRange`](/docs/schema/inputs/date-time-range))
