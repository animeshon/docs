---
id: string-term-filter
title: StringTermFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type StringTermFilter {
  allofterms: String
  anyofterms: String
}
```

### Fields

#### `allofterms` ([`String`](/docs/schema/scalars/string))

#### `anyofterms` ([`String`](/docs/schema/scalars/string))
