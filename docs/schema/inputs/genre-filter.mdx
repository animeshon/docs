---
id: genre-filter
title: GenreFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type GenreFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  has: GenreHasFilter
  and: [GenreFilter]
  or: [GenreFilter]
  not: GenreFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `has` ([`GenreHasFilter`](/docs/schema/enums/genre-has-filter))

#### `and` ([`[GenreFilter]`](/docs/schema/inputs/genre-filter))

#### `or` ([`[GenreFilter]`](/docs/schema/inputs/genre-filter))

#### `not` ([`GenreFilter`](/docs/schema/inputs/genre-filter))
