---
id: float-range
title: FloatRange
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type FloatRange {
  min: Float!
  max: Float!
}
```

### Fields

#### `min` ([`Float!`](/docs/schema/scalars/float))

#### `max` ([`Float!`](/docs/schema/scalars/float))
