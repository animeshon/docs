---
id: release-filter
title: ReleaseFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ReleaseFilter {
  censorship: CensorshipType_hash
  has: ReleaseHasFilter
  and: [ReleaseFilter]
  or: [ReleaseFilter]
  not: ReleaseFilter
}
```

### Fields

#### `censorship` ([`CensorshipType_hash`](/docs/schema/inputs/censorship-type-hash))

#### `has` ([`ReleaseHasFilter`](/docs/schema/enums/release-has-filter))

#### `and` ([`[ReleaseFilter]`](/docs/schema/inputs/release-filter))

#### `or` ([`[ReleaseFilter]`](/docs/schema/inputs/release-filter))

#### `not` ([`ReleaseFilter`](/docs/schema/inputs/release-filter))
