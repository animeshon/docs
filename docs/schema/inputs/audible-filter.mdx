---
id: audible-filter
title: AudibleFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type AudibleFilter {
  has: AudibleHasFilter
  and: [AudibleFilter]
  or: [AudibleFilter]
  not: AudibleFilter
}
```

### Fields

#### `has` ([`AudibleHasFilter`](/docs/schema/enums/audible-has-filter))

#### `and` ([`[AudibleFilter]`](/docs/schema/inputs/audible-filter))

#### `or` ([`[AudibleFilter]`](/docs/schema/inputs/audible-filter))

#### `not` ([`AudibleFilter`](/docs/schema/inputs/audible-filter))
