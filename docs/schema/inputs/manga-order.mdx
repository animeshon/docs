---
id: manga-order
title: MangaOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type MangaOrder {
  asc: MangaOrderable
  desc: MangaOrderable
  then: MangaOrder
}
```

### Fields

#### `asc` ([`MangaOrderable`](/docs/schema/enums/manga-orderable))

#### `desc` ([`MangaOrderable`](/docs/schema/enums/manga-orderable))

#### `then` ([`MangaOrder`](/docs/schema/inputs/manga-order))
