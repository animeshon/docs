---
id: sound-track-type-hash
title: SoundTrackType_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SoundTrackType_hash {
  eq: SoundTrackType!
  in: [SoundTrackType!]
}
```

### Fields

#### `eq` ([`SoundTrackType!`](/docs/schema/enums/sound-track-type))

#### `in` ([`[SoundTrackType!]`](/docs/schema/enums/sound-track-type))
