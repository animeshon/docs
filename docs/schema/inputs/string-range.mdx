---
id: string-range
title: StringRange
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type StringRange {
  min: String!
  max: String!
}
```

### Fields

#### `min` ([`String!`](/docs/schema/scalars/string))

#### `max` ([`String!`](/docs/schema/scalars/string))
