---
id: universe-filter
title: UniverseFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type UniverseFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  has: UniverseHasFilter
  and: [UniverseFilter]
  or: [UniverseFilter]
  not: UniverseFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `has` ([`UniverseHasFilter`](/docs/schema/enums/universe-has-filter))

#### `and` ([`[UniverseFilter]`](/docs/schema/inputs/universe-filter))

#### `or` ([`[UniverseFilter]`](/docs/schema/inputs/universe-filter))

#### `not` ([`UniverseFilter`](/docs/schema/inputs/universe-filter))
