---
id: game-voiced-degree-hash
title: GameVoicedDegree_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type GameVoicedDegree_hash {
  eq: GameVoicedDegree!
  in: [GameVoicedDegree!]
}
```

### Fields

#### `eq` ([`GameVoicedDegree!`](/docs/schema/enums/game-voiced-degree))

#### `in` ([`[GameVoicedDegree!]`](/docs/schema/enums/game-voiced-degree))
