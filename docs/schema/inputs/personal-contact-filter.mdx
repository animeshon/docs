---
id: personal-contact-filter
title: PersonalContactFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PersonalContactFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  has: PersonalContactHasFilter
  and: [PersonalContactFilter]
  or: [PersonalContactFilter]
  not: PersonalContactFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `has` ([`PersonalContactHasFilter`](/docs/schema/enums/personal-contact-has-filter))

#### `and` ([`[PersonalContactFilter]`](/docs/schema/inputs/personal-contact-filter))

#### `or` ([`[PersonalContactFilter]`](/docs/schema/inputs/personal-contact-filter))

#### `not` ([`PersonalContactFilter`](/docs/schema/inputs/personal-contact-filter))
