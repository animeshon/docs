---
id: multi-polygon-ref
title: MultiPolygonRef
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type MultiPolygonRef {
  polygons: [PolygonRef!]!
}
```

### Fields

#### `polygons` ([`[PolygonRef!]!`](/docs/schema/inputs/polygon-ref))
