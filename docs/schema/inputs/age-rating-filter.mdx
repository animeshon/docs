---
id: age-rating-filter
title: AgeRatingFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type AgeRatingFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  tag: StringTermFilter
  has: AgeRatingHasFilter
  and: [AgeRatingFilter]
  or: [AgeRatingFilter]
  not: AgeRatingFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `tag` ([`StringTermFilter`](/docs/schema/inputs/string-term-filter))

#### `has` ([`AgeRatingHasFilter`](/docs/schema/enums/age-rating-has-filter))

#### `and` ([`[AgeRatingFilter]`](/docs/schema/inputs/age-rating-filter))

#### `or` ([`[AgeRatingFilter]`](/docs/schema/inputs/age-rating-filter))

#### `not` ([`AgeRatingFilter`](/docs/schema/inputs/age-rating-filter))
