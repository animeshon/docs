---
id: video-filter
title: VideoFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type VideoFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  has: VideoHasFilter
  and: [VideoFilter]
  or: [VideoFilter]
  not: VideoFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `has` ([`VideoHasFilter`](/docs/schema/enums/video-has-filter))

#### `and` ([`[VideoFilter]`](/docs/schema/inputs/video-filter))

#### `or` ([`[VideoFilter]`](/docs/schema/inputs/video-filter))

#### `not` ([`VideoFilter`](/docs/schema/inputs/video-filter))
