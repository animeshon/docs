---
id: relation-label-order
title: RelationLabelOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type RelationLabelOrder {
  asc: RelationLabelOrderable
  desc: RelationLabelOrderable
  then: RelationLabelOrder
}
```

### Fields

#### `asc` ([`RelationLabelOrderable`](/docs/schema/enums/relation-label-orderable))

#### `desc` ([`RelationLabelOrderable`](/docs/schema/enums/relation-label-orderable))

#### `then` ([`RelationLabelOrder`](/docs/schema/inputs/relation-label-order))
