---
id: email-address-order
title: EmailAddressOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type EmailAddressOrder {
  asc: EmailAddressOrderable
  desc: EmailAddressOrderable
  then: EmailAddressOrder
}
```

### Fields

#### `asc` ([`EmailAddressOrderable`](/docs/schema/enums/email-address-orderable))

#### `desc` ([`EmailAddressOrderable`](/docs/schema/enums/email-address-orderable))

#### `then` ([`EmailAddressOrder`](/docs/schema/inputs/email-address-order))
