---
id: circle-order
title: CircleOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CircleOrder {
  asc: CircleOrderable
  desc: CircleOrderable
  then: CircleOrder
}
```

### Fields

#### `asc` ([`CircleOrderable`](/docs/schema/enums/circle-orderable))

#### `desc` ([`CircleOrderable`](/docs/schema/enums/circle-orderable))

#### `then` ([`CircleOrder`](/docs/schema/inputs/circle-order))
