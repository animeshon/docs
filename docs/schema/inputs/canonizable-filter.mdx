---
id: canonizable-filter
title: CanonizableFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CanonizableFilter {
  has: CanonizableHasFilter
  and: [CanonizableFilter]
  or: [CanonizableFilter]
  not: CanonizableFilter
}
```

### Fields

#### `has` ([`CanonizableHasFilter`](/docs/schema/enums/canonizable-has-filter))

#### `and` ([`[CanonizableFilter]`](/docs/schema/inputs/canonizable-filter))

#### `or` ([`[CanonizableFilter]`](/docs/schema/inputs/canonizable-filter))

#### `not` ([`CanonizableFilter`](/docs/schema/inputs/canonizable-filter))
