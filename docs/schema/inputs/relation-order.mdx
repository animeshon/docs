---
id: relation-order
title: RelationOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type RelationOrder {
  asc: RelationOrderable
  desc: RelationOrderable
  then: RelationOrder
}
```

### Fields

#### `asc` ([`RelationOrderable`](/docs/schema/enums/relation-orderable))

#### `desc` ([`RelationOrderable`](/docs/schema/enums/relation-orderable))

#### `then` ([`RelationOrder`](/docs/schema/inputs/relation-order))
