---
id: person-filter
title: PersonFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PersonFilter {
  birthday: DateTimeFilter
  id: StringHashFilter
  selfLink: StringHashFilter
  has: PersonHasFilter
  and: [PersonFilter]
  or: [PersonFilter]
  not: PersonFilter
}
```

### Fields

#### `birthday` ([`DateTimeFilter`](/docs/schema/inputs/date-time-filter))

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `has` ([`PersonHasFilter`](/docs/schema/enums/person-has-filter))

#### `and` ([`[PersonFilter]`](/docs/schema/inputs/person-filter))

#### `or` ([`[PersonFilter]`](/docs/schema/inputs/person-filter))

#### `not` ([`PersonFilter`](/docs/schema/inputs/person-filter))
