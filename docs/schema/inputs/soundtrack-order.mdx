---
id: soundtrack-order
title: SoundtrackOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SoundtrackOrder {
  asc: SoundtrackOrderable
  desc: SoundtrackOrderable
  then: SoundtrackOrder
}
```

### Fields

#### `asc` ([`SoundtrackOrderable`](/docs/schema/enums/soundtrack-orderable))

#### `desc` ([`SoundtrackOrderable`](/docs/schema/enums/soundtrack-orderable))

#### `then` ([`SoundtrackOrder`](/docs/schema/inputs/soundtrack-order))
