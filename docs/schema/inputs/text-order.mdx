---
id: text-order
title: TextOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type TextOrder {
  asc: TextOrderable
  desc: TextOrderable
  then: TextOrder
}
```

### Fields

#### `asc` ([`TextOrderable`](/docs/schema/enums/text-orderable))

#### `desc` ([`TextOrderable`](/docs/schema/enums/text-orderable))

#### `then` ([`TextOrder`](/docs/schema/inputs/text-order))
