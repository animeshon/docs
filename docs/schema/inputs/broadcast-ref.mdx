---
id: broadcast-ref
title: BroadcastRef
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type BroadcastRef {
  id: String
  selfLink: String
  createdAt: DateTime
  modifiedAt: DateTime
  version: String
  content: EpisodeRef
  broadcaster: OrganizationRef
  from: DateTime
  to: DateTime
  localization: LocalizationRef
}
```

### Fields

#### `id` ([`String`](/docs/schema/scalars/string))

#### `selfLink` ([`String`](/docs/schema/scalars/string))

#### `createdAt` ([`DateTime`](/docs/schema/scalars/date-time))

#### `modifiedAt` ([`DateTime`](/docs/schema/scalars/date-time))

#### `version` ([`String`](/docs/schema/scalars/string))

#### `content` ([`EpisodeRef`](/docs/schema/inputs/episode-ref))

#### `broadcaster` ([`OrganizationRef`](/docs/schema/inputs/organization-ref))

#### `from` ([`DateTime`](/docs/schema/scalars/date-time))

#### `to` ([`DateTime`](/docs/schema/scalars/date-time))

#### `localization` ([`LocalizationRef`](/docs/schema/inputs/localization-ref))
