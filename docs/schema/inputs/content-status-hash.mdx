---
id: content-status-hash
title: ContentStatus_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ContentStatus_hash {
  eq: ContentStatus!
  in: [ContentStatus!]
}
```

### Fields

#### `eq` ([`ContentStatus!`](/docs/schema/enums/content-status))

#### `in` ([`[ContentStatus!]`](/docs/schema/enums/content-status))
