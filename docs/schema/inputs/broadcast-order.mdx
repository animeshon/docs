---
id: broadcast-order
title: BroadcastOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type BroadcastOrder {
  asc: BroadcastOrderable
  desc: BroadcastOrderable
  then: BroadcastOrder
}
```

### Fields

#### `asc` ([`BroadcastOrderable`](/docs/schema/enums/broadcast-orderable))

#### `desc` ([`BroadcastOrderable`](/docs/schema/enums/broadcast-orderable))

#### `then` ([`BroadcastOrder`](/docs/schema/inputs/broadcast-order))
