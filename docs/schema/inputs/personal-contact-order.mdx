---
id: personal-contact-order
title: PersonalContactOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PersonalContactOrder {
  asc: PersonalContactOrderable
  desc: PersonalContactOrderable
  then: PersonalContactOrder
}
```

### Fields

#### `asc` ([`PersonalContactOrderable`](/docs/schema/enums/personal-contact-orderable))

#### `desc` ([`PersonalContactOrderable`](/docs/schema/enums/personal-contact-orderable))

#### `then` ([`PersonalContactOrder`](/docs/schema/inputs/personal-contact-order))
