---
id: language-order
title: LanguageOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type LanguageOrder {
  asc: LanguageOrderable
  desc: LanguageOrderable
  then: LanguageOrder
}
```

### Fields

#### `asc` ([`LanguageOrderable`](/docs/schema/enums/language-orderable))

#### `desc` ([`LanguageOrderable`](/docs/schema/enums/language-orderable))

#### `then` ([`LanguageOrder`](/docs/schema/inputs/language-order))
