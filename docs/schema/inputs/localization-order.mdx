---
id: localization-order
title: LocalizationOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type LocalizationOrder {
  asc: LocalizationOrderable
  desc: LocalizationOrderable
  then: LocalizationOrder
}
```

### Fields

#### `asc` ([`LocalizationOrderable`](/docs/schema/enums/localization-orderable))

#### `desc` ([`LocalizationOrderable`](/docs/schema/enums/localization-orderable))

#### `then` ([`LocalizationOrder`](/docs/schema/inputs/localization-order))
