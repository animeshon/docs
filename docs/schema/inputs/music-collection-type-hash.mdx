---
id: music-collection-type-hash
title: MusicCollectionType_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type MusicCollectionType_hash {
  eq: MusicCollectionType!
  in: [MusicCollectionType!]
}
```

### Fields

#### `eq` ([`MusicCollectionType!`](/docs/schema/enums/music-collection-type))

#### `in` ([`[MusicCollectionType!]`](/docs/schema/enums/music-collection-type))
