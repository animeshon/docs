---
id: releasable-filter
title: ReleasableFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ReleasableFilter {
  not: ReleasableFilter
}
```

### Fields

#### `not` ([`ReleasableFilter`](/docs/schema/inputs/releasable-filter))
