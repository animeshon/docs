---
id: video-wrapper-order
title: VideoWrapperOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type VideoWrapperOrder {
  asc: VideoWrapperOrderable
  desc: VideoWrapperOrderable
  then: VideoWrapperOrder
}
```

### Fields

#### `asc` ([`VideoWrapperOrderable`](/docs/schema/enums/video-wrapper-orderable))

#### `desc` ([`VideoWrapperOrderable`](/docs/schema/enums/video-wrapper-orderable))

#### `then` ([`VideoWrapperOrder`](/docs/schema/inputs/video-wrapper-order))
