---
id: character-order
title: CharacterOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CharacterOrder {
  asc: CharacterOrderable
  desc: CharacterOrderable
  then: CharacterOrder
}
```

### Fields

#### `asc` ([`CharacterOrderable`](/docs/schema/enums/character-orderable))

#### `desc` ([`CharacterOrderable`](/docs/schema/enums/character-orderable))

#### `then` ([`CharacterOrder`](/docs/schema/inputs/character-order))
