---
id: audio-order
title: AudioOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type AudioOrder {
  asc: AudioOrderable
  desc: AudioOrderable
  then: AudioOrder
}
```

### Fields

#### `asc` ([`AudioOrderable`](/docs/schema/enums/audio-orderable))

#### `desc` ([`AudioOrderable`](/docs/schema/enums/audio-orderable))

#### `then` ([`AudioOrder`](/docs/schema/inputs/audio-order))
