---
id: universe-order
title: UniverseOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type UniverseOrder {
  asc: UniverseOrderable
  desc: UniverseOrderable
  then: UniverseOrder
}
```

### Fields

#### `asc` ([`UniverseOrderable`](/docs/schema/enums/universe-orderable))

#### `desc` ([`UniverseOrderable`](/docs/schema/enums/universe-orderable))

#### `then` ([`UniverseOrder`](/docs/schema/inputs/universe-order))
