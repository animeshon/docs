---
id: relation-filter
title: RelationFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type RelationFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  from: DateTimeFilter
  to: DateTimeFilter
  has: RelationHasFilter
  and: [RelationFilter]
  or: [RelationFilter]
  not: RelationFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `from` ([`DateTimeFilter`](/docs/schema/inputs/date-time-filter))

#### `to` ([`DateTimeFilter`](/docs/schema/inputs/date-time-filter))

#### `has` ([`RelationHasFilter`](/docs/schema/enums/relation-has-filter))

#### `and` ([`[RelationFilter]`](/docs/schema/inputs/relation-filter))

#### `or` ([`[RelationFilter]`](/docs/schema/inputs/relation-filter))

#### `not` ([`RelationFilter`](/docs/schema/inputs/relation-filter))
