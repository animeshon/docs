---
id: date-time-range
title: DateTimeRange
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type DateTimeRange {
  min: DateTime!
  max: DateTime!
}
```

### Fields

#### `min` ([`DateTime!`](/docs/schema/scalars/date-time))

#### `max` ([`DateTime!`](/docs/schema/scalars/date-time))
