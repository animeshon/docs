---
id: running-filter
title: RunningFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type RunningFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  from: DateTimeFilter
  to: DateTimeFilter
  has: RunningHasFilter
  and: [RunningFilter]
  or: [RunningFilter]
  not: RunningFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `from` ([`DateTimeFilter`](/docs/schema/inputs/date-time-filter))

#### `to` ([`DateTimeFilter`](/docs/schema/inputs/date-time-filter))

#### `has` ([`RunningHasFilter`](/docs/schema/enums/running-has-filter))

#### `and` ([`[RunningFilter]`](/docs/schema/inputs/running-filter))

#### `or` ([`[RunningFilter]`](/docs/schema/inputs/running-filter))

#### `not` ([`RunningFilter`](/docs/schema/inputs/running-filter))
