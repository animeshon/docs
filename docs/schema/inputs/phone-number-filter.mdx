---
id: phone-number-filter
title: PhoneNumberFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PhoneNumberFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  has: PhoneNumberHasFilter
  and: [PhoneNumberFilter]
  or: [PhoneNumberFilter]
  not: PhoneNumberFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `has` ([`PhoneNumberHasFilter`](/docs/schema/enums/phone-number-has-filter))

#### `and` ([`[PhoneNumberFilter]`](/docs/schema/inputs/phone-number-filter))

#### `or` ([`[PhoneNumberFilter]`](/docs/schema/inputs/phone-number-filter))

#### `not` ([`PhoneNumberFilter`](/docs/schema/inputs/phone-number-filter))
