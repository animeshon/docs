---
id: address-order
title: AddressOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type AddressOrder {
  asc: AddressOrderable
  desc: AddressOrderable
  then: AddressOrder
}
```

### Fields

#### `asc` ([`AddressOrderable`](/docs/schema/enums/address-orderable))

#### `desc` ([`AddressOrderable`](/docs/schema/enums/address-orderable))

#### `then` ([`AddressOrder`](/docs/schema/inputs/address-order))
