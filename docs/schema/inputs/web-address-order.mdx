---
id: web-address-order
title: WebAddressOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type WebAddressOrder {
  asc: WebAddressOrderable
  desc: WebAddressOrderable
  then: WebAddressOrder
}
```

### Fields

#### `asc` ([`WebAddressOrderable`](/docs/schema/enums/web-address-orderable))

#### `desc` ([`WebAddressOrderable`](/docs/schema/enums/web-address-orderable))

#### `then` ([`WebAddressOrder`](/docs/schema/inputs/web-address-order))
