---
id: string-reg-exp-filter
title: StringRegExpFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type StringRegExpFilter {
  regexp: String
}
```

### Fields

#### `regexp` ([`String`](/docs/schema/scalars/string))
