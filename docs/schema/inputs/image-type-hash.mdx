---
id: image-type-hash
title: ImageType_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ImageType_hash {
  eq: ImageType!
  in: [ImageType!]
}
```

### Fields

#### `eq` ([`ImageType!`](/docs/schema/enums/image-type))

#### `in` ([`[ImageType!]`](/docs/schema/enums/image-type))
