---
id: file-order
title: FileOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type FileOrder {
  asc: FileOrderable
  desc: FileOrderable
  then: FileOrder
}
```

### Fields

#### `asc` ([`FileOrderable`](/docs/schema/enums/file-orderable))

#### `desc` ([`FileOrderable`](/docs/schema/enums/file-orderable))

#### `then` ([`FileOrder`](/docs/schema/inputs/file-order))
