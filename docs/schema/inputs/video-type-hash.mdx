---
id: video-type-hash
title: VideoType_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type VideoType_hash {
  eq: VideoType!
  in: [VideoType!]
}
```

### Fields

#### `eq` ([`VideoType!`](/docs/schema/enums/video-type))

#### `in` ([`[VideoType!]`](/docs/schema/enums/video-type))
