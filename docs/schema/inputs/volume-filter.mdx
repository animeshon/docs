---
id: volume-filter
title: VolumeFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type VolumeFilter {
  censorship: CensorshipType_hash
  id: StringHashFilter
  selfLink: StringHashFilter
  identifier: StringHashFilter
  has: VolumeHasFilter
  and: [VolumeFilter]
  or: [VolumeFilter]
  not: VolumeFilter
}
```

### Fields

#### `censorship` ([`CensorshipType_hash`](/docs/schema/inputs/censorship-type-hash))

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `identifier` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `has` ([`VolumeHasFilter`](/docs/schema/enums/volume-has-filter))

#### `and` ([`[VolumeFilter]`](/docs/schema/inputs/volume-filter))

#### `or` ([`[VolumeFilter]`](/docs/schema/inputs/volume-filter))

#### `not` ([`VolumeFilter`](/docs/schema/inputs/volume-filter))
