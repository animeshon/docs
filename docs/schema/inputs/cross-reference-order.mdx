---
id: cross-reference-order
title: CrossReferenceOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CrossReferenceOrder {
  asc: CrossReferenceOrderable
  desc: CrossReferenceOrderable
  then: CrossReferenceOrder
}
```

### Fields

#### `asc` ([`CrossReferenceOrderable`](/docs/schema/enums/cross-reference-orderable))

#### `desc` ([`CrossReferenceOrderable`](/docs/schema/enums/cross-reference-orderable))

#### `then` ([`CrossReferenceOrder`](/docs/schema/inputs/cross-reference-order))
