---
id: role-type-hash
title: RoleType_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type RoleType_hash {
  eq: RoleType!
  in: [RoleType!]
}
```

### Fields

#### `eq` ([`RoleType!`](/docs/schema/enums/role-type))

#### `in` ([`[RoleType!]`](/docs/schema/enums/role-type))
