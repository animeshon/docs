---
id: point-list-ref
title: PointListRef
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PointListRef {
  points: [PointRef!]!
}
```

### Fields

#### `points` ([`[PointRef!]!`](/docs/schema/inputs/point-ref))
