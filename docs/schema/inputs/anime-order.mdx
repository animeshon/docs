---
id: anime-order
title: AnimeOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type AnimeOrder {
  asc: AnimeOrderable
  desc: AnimeOrderable
  then: AnimeOrder
}
```

### Fields

#### `asc` ([`AnimeOrderable`](/docs/schema/enums/anime-orderable))

#### `desc` ([`AnimeOrderable`](/docs/schema/enums/anime-orderable))

#### `then` ([`AnimeOrder`](/docs/schema/inputs/anime-order))
