---
id: near-filter
title: NearFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type NearFilter {
  distance: Float!
  coordinate: PointRef!
}
```

### Fields

#### `distance` ([`Float!`](/docs/schema/scalars/float))

#### `coordinate` ([`PointRef!`](/docs/schema/inputs/point-ref))
