---
id: canonical-order
title: CanonicalOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CanonicalOrder {
  asc: CanonicalOrderable
  desc: CanonicalOrderable
  then: CanonicalOrder
}
```

### Fields

#### `asc` ([`CanonicalOrderable`](/docs/schema/enums/canonical-orderable))

#### `desc` ([`CanonicalOrderable`](/docs/schema/enums/canonical-orderable))

#### `then` ([`CanonicalOrder`](/docs/schema/inputs/canonical-order))
