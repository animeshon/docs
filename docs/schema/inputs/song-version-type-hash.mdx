---
id: song-version-type-hash
title: SongVersionType_hash
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SongVersionType_hash {
  eq: SongVersionType!
  in: [SongVersionType!]
}
```

### Fields

#### `eq` ([`SongVersionType!`](/docs/schema/enums/song-version-type))

#### `in` ([`[SongVersionType!]`](/docs/schema/enums/song-version-type))
