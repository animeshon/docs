---
id: chapter-order
title: ChapterOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ChapterOrder {
  asc: ChapterOrderable
  desc: ChapterOrderable
  then: ChapterOrder
}
```

### Fields

#### `asc` ([`ChapterOrderable`](/docs/schema/enums/chapter-orderable))

#### `desc` ([`ChapterOrderable`](/docs/schema/enums/chapter-orderable))

#### `then` ([`ChapterOrder`](/docs/schema/inputs/chapter-order))
