---
id: broadcast-filter
title: BroadcastFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type BroadcastFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  from: DateTimeFilter
  to: DateTimeFilter
  has: BroadcastHasFilter
  and: [BroadcastFilter]
  or: [BroadcastFilter]
  not: BroadcastFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `from` ([`DateTimeFilter`](/docs/schema/inputs/date-time-filter))

#### `to` ([`DateTimeFilter`](/docs/schema/inputs/date-time-filter))

#### `has` ([`BroadcastHasFilter`](/docs/schema/enums/broadcast-has-filter))

#### `and` ([`[BroadcastFilter]`](/docs/schema/inputs/broadcast-filter))

#### `or` ([`[BroadcastFilter]`](/docs/schema/inputs/broadcast-filter))

#### `not` ([`BroadcastFilter`](/docs/schema/inputs/broadcast-filter))
