---
id: contains-filter
title: ContainsFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ContainsFilter {
  point: PointRef
  polygon: PolygonRef
}
```

### Fields

#### `point` ([`PointRef`](/docs/schema/inputs/point-ref))

#### `polygon` ([`PolygonRef`](/docs/schema/inputs/polygon-ref))
