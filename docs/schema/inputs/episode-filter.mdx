---
id: episode-filter
title: EpisodeFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type EpisodeFilter {
  id: StringHashFilter
  selfLink: StringHashFilter
  type: EpisodeType_hash
  index: StringHashFilter
  releaseDate: DateTimeFilter
  has: EpisodeHasFilter
  and: [EpisodeFilter]
  or: [EpisodeFilter]
  not: EpisodeFilter
}
```

### Fields

#### `id` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `selfLink` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `type` ([`EpisodeType_hash`](/docs/schema/inputs/episode-type-hash))

#### `index` ([`StringHashFilter`](/docs/schema/inputs/string-hash-filter))

#### `releaseDate` ([`DateTimeFilter`](/docs/schema/inputs/date-time-filter))

#### `has` ([`EpisodeHasFilter`](/docs/schema/enums/episode-has-filter))

#### `and` ([`[EpisodeFilter]`](/docs/schema/inputs/episode-filter))

#### `or` ([`[EpisodeFilter]`](/docs/schema/inputs/episode-filter))

#### `not` ([`EpisodeFilter`](/docs/schema/inputs/episode-filter))
