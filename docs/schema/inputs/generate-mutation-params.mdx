---
id: generate-mutation-params
title: GenerateMutationParams
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type GenerateMutationParams {
  add: Boolean
  update: Boolean
  delete: Boolean
}
```

### Fields

#### `add` ([`Boolean`](/docs/schema/scalars/boolean))

#### `update` ([`Boolean`](/docs/schema/scalars/boolean))

#### `delete` ([`Boolean`](/docs/schema/scalars/boolean))
