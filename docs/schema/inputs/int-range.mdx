---
id: int-range
title: IntRange
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type IntRange {
  min: Int!
  max: Int!
}
```

### Fields

#### `min` ([`Int!`](/docs/schema/scalars/int))

#### `max` ([`Int!`](/docs/schema/scalars/int))
