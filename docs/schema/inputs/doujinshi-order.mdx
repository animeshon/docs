---
id: doujinshi-order
title: DoujinshiOrder
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type DoujinshiOrder {
  asc: DoujinshiOrderable
  desc: DoujinshiOrderable
  then: DoujinshiOrder
}
```

### Fields

#### `asc` ([`DoujinshiOrderable`](/docs/schema/enums/doujinshi-orderable))

#### `desc` ([`DoujinshiOrderable`](/docs/schema/enums/doujinshi-orderable))

#### `then` ([`DoujinshiOrder`](/docs/schema/inputs/doujinshi-order))
