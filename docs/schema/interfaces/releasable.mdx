---
id: releasable
title: Releasable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
interface Releasable {
  releases(
    filter: ReleaseFilter
    order: ReleaseOrder
    first: Int = 0
    offset: Int = 0
  ): [Release!]
  releasesAggregate(filter: ReleaseFilter): ReleaseAggregateResult
}
```

### Fields

#### `releases` ([`[Release!]`](/docs/schema/interfaces/release))

- ##### `filter` ([`ReleaseFilter`](/docs/schema/inputs/release-filter))

- ##### `order` ([`ReleaseOrder`](/docs/schema/inputs/release-order))

- ##### `first` ([`Int`](/docs/schema/scalars/int))

- ##### `offset` ([`Int`](/docs/schema/scalars/int))

#### `releasesAggregate` ([`ReleaseAggregateResult`](/docs/schema/objects/release-aggregate-result))

- ##### `filter` ([`ReleaseFilter`](/docs/schema/inputs/release-filter))
