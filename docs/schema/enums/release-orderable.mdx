---
id: release-orderable
title: ReleaseOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ReleaseOrderable {
  releaseDate
  ean10
  upce
  upca
  ean13
  sku
}
```

### Values

#### `releaseDate`

#### `ean10`

#### `upce`

#### `upca`

#### `ean13`

#### `sku`
