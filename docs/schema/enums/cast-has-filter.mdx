---
id: cast-has-filter
title: CastHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum CastHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  character
  content
  relation
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `character`

#### `content`

#### `relation`
