---
id: medium-type
title: MediumType
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum MediumType {
  UNKNOWN
  OTHER
  BD
  CARTRIDGE
  CD
  COLLECTOR
  DIGITAL_RELEASE
  DVD
  FLOPPY
  GD_ROM
  MEMORY
  N_OPTICAL
  PHYSICAL
  PRINTED
  UMD
}
```

### Values

#### `UNKNOWN`

#### `OTHER`

#### `BD`

#### `CARTRIDGE`

#### `CD`

#### `COLLECTOR`

#### `DIGITAL_RELEASE`

#### `DVD`

#### `FLOPPY`

#### `GD_ROM`

#### `MEMORY`

#### `N_OPTICAL`

#### `PHYSICAL`

#### `PRINTED`

#### `UMD`
