---
id: convention-orderable
title: ConventionOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ConventionOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  from
  to
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `from`

#### `to`
