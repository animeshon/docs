---
id: with-relation-has-filter
title: WithRelationHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum WithRelationHasFilter {
  relations
}
```

### Values

#### `relations`
