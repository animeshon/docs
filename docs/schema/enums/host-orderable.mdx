---
id: host-orderable
title: HostOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum HostOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  IP
  eTLD
  domain
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `IP`

#### `eTLD`

#### `domain`
