---
id: http-method
title: HTTPMethod
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum HTTPMethod {
  GET
  POST
  PUT
  PATCH
  DELETE
}
```

### Values

#### `GET`

#### `POST`

#### `PUT`

#### `PATCH`

#### `DELETE`
