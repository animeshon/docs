---
id: music-collection-track-orderable
title: MusicCollectionTrackOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum MusicCollectionTrackOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  identifier
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `identifier`
