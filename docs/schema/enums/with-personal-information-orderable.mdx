---
id: with-personal-information-orderable
title: WithPersonalInformationOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum WithPersonalInformationOrderable {
  birthdayFallback
  birthday
  age
  height
  weight
  bust
  waist
  hips
}
```

### Values

#### `birthdayFallback`

#### `birthday`

#### `age`

#### `height`

#### `weight`

#### `bust`

#### `waist`

#### `hips`
