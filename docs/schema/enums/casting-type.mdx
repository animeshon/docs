---
id: casting-type
title: CastingType
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum CastingType {
  UNKNOWN
  MAIN
  SUPPORT
  APPEARS
}
```

### Values

#### `UNKNOWN`

#### `MAIN`

#### `SUPPORT`

#### `APPEARS`
