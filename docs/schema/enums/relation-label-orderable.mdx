---
id: relation-label-orderable
title: RelationLabelOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum RelationLabelOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  tag
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `tag`
