---
id: macro-language-orderable
title: MacroLanguageOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum MacroLanguageOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  status
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `status`
