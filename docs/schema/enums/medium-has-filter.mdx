---
id: medium-has-filter
title: MediumHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum MediumHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  type
  quantity
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `type`

#### `quantity`
