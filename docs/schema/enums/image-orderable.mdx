---
id: image-orderable
title: ImageOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ImageOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  width
  height
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `width`

#### `height`
