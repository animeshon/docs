---
id: chapter-type
title: ChapterType
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ChapterType {
  REGULAR
  EXTRA
}
```

### Values

#### `REGULAR`

#### `EXTRA`
