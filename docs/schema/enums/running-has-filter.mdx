---
id: running-has-filter
title: RunningHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum RunningHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  from
  to
  localization
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `from`

#### `to`

#### `localization`
