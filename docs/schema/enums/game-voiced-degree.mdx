---
id: game-voiced-degree
title: GameVoicedDegree
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum GameVoicedDegree {
  UNKNOWN
  NONE
  ERO_ONLY
  PARTIAL
  FULL
}
```

### Values

#### `UNKNOWN`

#### `NONE`

#### `ERO_ONLY`

#### `PARTIAL`

#### `FULL`
