---
id: macro-language-has-filter
title: MacroLanguageHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum MacroLanguageHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  language
  status
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `language`

#### `status`
