---
id: video-has-filter
title: VideoHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum VideoHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  files
  width
  height
  aspectRatio
  duration
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `files`

#### `width`

#### `height`

#### `aspectRatio`

#### `duration`
