---
id: relation-label-has-filter
title: RelationLabelHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum RelationLabelHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  names
  aliases
  description
  in
  tag
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `names`

#### `aliases`

#### `description`

#### `in`

#### `tag`
