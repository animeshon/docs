---
id: visual-novel-release-orderable
title: VisualNovelReleaseOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum VisualNovelReleaseOrderable {
  releaseDate
  ean10
  upce
  upca
  ean13
  sku
  id
  selfLink
  createdAt
  modifiedAt
  version
  resX
  resY
  engine
}
```

### Values

#### `releaseDate`

#### `ean10`

#### `upce`

#### `upca`

#### `ean13`

#### `sku`

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `resX`

#### `resY`

#### `engine`
