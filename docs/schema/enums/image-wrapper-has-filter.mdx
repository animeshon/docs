---
id: image-wrapper-has-filter
title: ImageWrapperHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ImageWrapperHasFilter {
  restrictions
  ageRatings
  id
  selfLink
  createdAt
  modifiedAt
  version
  image
  thumbnails
  type
  localization
}
```

### Values

#### `restrictions`

#### `ageRatings`

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `image`

#### `thumbnails`

#### `type`

#### `localization`
