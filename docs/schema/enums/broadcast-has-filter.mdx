---
id: broadcast-has-filter
title: BroadcastHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum BroadcastHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  content
  broadcaster
  from
  to
  localization
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `content`

#### `broadcaster`

#### `from`

#### `to`

#### `localization`
