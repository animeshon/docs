---
id: character-orderable
title: CharacterOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum CharacterOrderable {
  birthdayFallback
  birthday
  age
  height
  weight
  bust
  waist
  hips
  id
  selfLink
  createdAt
  modifiedAt
  version
}
```

### Values

#### `birthdayFallback`

#### `birthday`

#### `age`

#### `height`

#### `weight`

#### `bust`

#### `waist`

#### `hips`

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`
