---
id: writing-system
title: WritingSystem
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum WritingSystem {
  ALPHABETIC
  FURIGANA_FULL
  FURIGANA_PARTIAL
  KANJI
}
```

### Values

#### `ALPHABETIC`

#### `FURIGANA_FULL`

#### `FURIGANA_PARTIAL`

#### `KANJI`
