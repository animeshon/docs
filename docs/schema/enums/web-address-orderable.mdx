---
id: web-address-orderable
title: WebAddressOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum WebAddressOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  formattedAddress
  path
  query
  fragment
  lastSeenAvailable
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `formattedAddress`

#### `path`

#### `query`

#### `fragment`

#### `lastSeenAvailable`
