---
id: song-orderable
title: SongOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum SongOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  releaseDate
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `releaseDate`
