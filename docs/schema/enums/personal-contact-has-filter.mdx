---
id: personal-contact-has-filter
title: PersonalContactHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum PersonalContactHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  email
  phone
  fax
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `email`

#### `phone`

#### `fax`
