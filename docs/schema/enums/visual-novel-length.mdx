---
id: visual-novel-length
title: VisualNovelLength
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum VisualNovelLength {
  UNKNOWN
  VERY_SHORT
  SHORT
  MEDIUM
  LONG
  VERY_LONG
}
```

### Values

#### `UNKNOWN`

#### `VERY_SHORT`

#### `SHORT`

#### `MEDIUM`

#### `LONG`

#### `VERY_LONG`
