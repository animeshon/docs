---
id: age-rating-has-filter
title: AgeRatingHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum AgeRatingHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  country
  age
  tag
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `country`

#### `age`

#### `tag`
