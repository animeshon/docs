---
id: game-animaton-degree
title: GameAnimatonDegree
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum GameAnimatonDegree {
  UNKNOWN
  NONE
  SIMPLE
  PARTIAL
  FULL
}
```

### Values

#### `UNKNOWN`

#### `NONE`

#### `SIMPLE`

#### `PARTIAL`

#### `FULL`
