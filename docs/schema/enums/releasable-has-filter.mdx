---
id: releasable-has-filter
title: ReleasableHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ReleasableHasFilter {
  releases
}
```

### Values

#### `releases`
