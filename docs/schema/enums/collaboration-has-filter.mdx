---
id: collaboration-has-filter
title: CollaborationHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum CollaborationHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  collaborator
  content
  role
  localization
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `collaborator`

#### `content`

#### `role`

#### `localization`
