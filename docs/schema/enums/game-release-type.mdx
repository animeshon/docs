---
id: game-release-type
title: GameReleaseType
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum GameReleaseType {
  UNKNOWN
  COMPLETE
  DLC
  PARTIAL
  TRIAL
}
```

### Values

#### `UNKNOWN`

#### `COMPLETE`

#### `DLC`

#### `PARTIAL`

#### `TRIAL`
