---
id: episode-orderable
title: EpisodeOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum EpisodeOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  index
  releaseDate
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `index`

#### `releaseDate`
