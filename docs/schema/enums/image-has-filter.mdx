---
id: image-has-filter
title: ImageHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ImageHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  files
  width
  height
  aspectRatio
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `files`

#### `width`

#### `height`

#### `aspectRatio`
