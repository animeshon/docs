---
id: video-wrapper-has-filter
title: VideoWrapperHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum VideoWrapperHasFilter {
  restrictions
  ageRatings
  id
  selfLink
  createdAt
  modifiedAt
  version
  video
  thumbnails
  type
  localization
}
```

### Values

#### `restrictions`

#### `ageRatings`

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `video`

#### `thumbnails`

#### `type`

#### `localization`
