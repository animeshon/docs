---
id: volume-orderable
title: VolumeOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum VolumeOrderable {
  releaseDate
  ean10
  upce
  upca
  ean13
  sku
  id
  selfLink
  createdAt
  modifiedAt
  version
  identifier
}
```

### Values

#### `releaseDate`

#### `ean10`

#### `upce`

#### `upca`

#### `ean13`

#### `sku`

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `identifier`
