---
id: script-orderable
title: ScriptOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ScriptOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  code
  number
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `code`

#### `number`
