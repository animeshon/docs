---
id: web-address-has-filter
title: WebAddressHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum WebAddressHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  formattedAddress
  host
  path
  query
  fragment
  lastSeenAvailable
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `formattedAddress`

#### `host`

#### `path`

#### `query`

#### `fragment`

#### `lastSeenAvailable`
