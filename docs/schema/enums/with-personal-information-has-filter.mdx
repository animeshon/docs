---
id: with-personal-information-has-filter
title: WithPersonalInformationHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum WithPersonalInformationHasFilter {
  birthdayFallback
  birthPlaceFallback
  hometownFallback
  birthday
  birthPlace
  hometown
  gender
  bloodType
  cupSize
  age
  height
  weight
  bust
  waist
  hips
}
```

### Values

#### `birthdayFallback`

#### `birthPlaceFallback`

#### `hometownFallback`

#### `birthday`

#### `birthPlace`

#### `hometown`

#### `gender`

#### `bloodType`

#### `cupSize`

#### `age`

#### `height`

#### `weight`

#### `bust`

#### `waist`

#### `hips`
