---
id: relation-has-filter
title: RelationHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum RelationHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  subject
  object
  label
  from
  to
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `subject`

#### `object`

#### `label`

#### `from`

#### `to`
