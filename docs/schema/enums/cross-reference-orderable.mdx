---
id: cross-reference-orderable
title: CrossReferenceOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum CrossReferenceOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  kind
  externalID
  namespace
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `kind`

#### `externalID`

#### `namespace`
