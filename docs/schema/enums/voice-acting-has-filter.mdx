---
id: voice-acting-has-filter
title: VoiceActingHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum VoiceActingHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  content
  voiced
  actor
  isPrimary
  localization
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `content`

#### `voiced`

#### `actor`

#### `isPrimary`

#### `localization`
