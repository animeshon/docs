---
id: localization-has-filter
title: LocalizationHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum LocalizationHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  tag
  country
  language
  script
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `tag`

#### `country`

#### `language`

#### `script`
