---
id: dgraph-index
title: DgraphIndex
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum DgraphIndex {
  int
  int64
  float
  bool
  hash
  exact
  term
  fulltext
  trigram
  regexp
  year
  month
  day
  hour
  geo
}
```

### Values

#### `int`

#### `int64`

#### `float`

#### `bool`

#### `hash`

#### `exact`

#### `term`

#### `fulltext`

#### `trigram`

#### `regexp`

#### `year`

#### `month`

#### `day`

#### `hour`

#### `geo`
