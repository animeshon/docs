---
id: country-orderable
title: CountryOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum CountryOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  code
  alpha2
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `code`

#### `alpha2`
