---
id: email-address-has-filter
title: EmailAddressHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum EmailAddressHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  username
  domain
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `username`

#### `domain`
