---
id: image-type
title: ImageType
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ImageType {
  COVER
  PROFILE
  REGULAR
  SCREENSHOT
}
```

### Values

#### `COVER`

#### `PROFILE`

#### `REGULAR`

#### `SCREENSHOT`
