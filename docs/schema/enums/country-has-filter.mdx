---
id: country-has-filter
title: CountryHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum CountryHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  names
  code
  alpha2
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `names`

#### `code`

#### `alpha2`
