---
id: audio-has-filter
title: AudioHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum AudioHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  files
  bitrate
  duration
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `files`

#### `bitrate`

#### `duration`
