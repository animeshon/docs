---
id: chapter-orderable
title: ChapterOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ChapterOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  identifier
  pageCount
  releaseDate
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `identifier`

#### `pageCount`

#### `releaseDate`
