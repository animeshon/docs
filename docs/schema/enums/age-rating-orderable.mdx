---
id: age-rating-orderable
title: AgeRatingOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum AgeRatingOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  age
  tag
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `age`

#### `tag`
