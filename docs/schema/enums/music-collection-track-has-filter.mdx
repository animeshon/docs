---
id: music-collection-track-has-filter
title: MusicCollectionTrackHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum MusicCollectionTrackHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  musicCollection
  song
  trackVersion
  identifier
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `musicCollection`

#### `song`

#### `trackVersion`

#### `identifier`
