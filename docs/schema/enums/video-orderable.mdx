---
id: video-orderable
title: VideoOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum VideoOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  width
  height
  duration
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `width`

#### `height`

#### `duration`
