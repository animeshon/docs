---
id: role-has-filter
title: RoleHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum RoleHasFilter {
  description
  in
}
```

### Values

#### `description`

#### `in`
