---
id: manga-has-filter
title: MangaHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum MangaHasFilter {
  restrictions
  chapters
  releases
  ageRatings
  status
  relations
  partOfCanonicals
  genres
  staff
  starring
  names
  aliases
  descriptions
  images
  crossrefs
  websites
  id
  selfLink
  createdAt
  modifiedAt
  version
  type
  volumes
  runnings
}
```

### Values

#### `restrictions`

#### `chapters`

#### `releases`

#### `ageRatings`

#### `status`

#### `relations`

#### `partOfCanonicals`

#### `genres`

#### `staff`

#### `starring`

#### `names`

#### `aliases`

#### `descriptions`

#### `images`

#### `crossrefs`

#### `websites`

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `type`

#### `volumes`

#### `runnings`
