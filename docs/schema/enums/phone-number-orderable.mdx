---
id: phone-number-orderable
title: PhoneNumberOrderable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum PhoneNumberOrderable {
  id
  selfLink
  createdAt
  modifiedAt
  version
  prefix
  number
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `prefix`

#### `number`
