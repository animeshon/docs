---
id: script-has-filter
title: ScriptHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ScriptHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  names
  code
  number
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `names`

#### `code`

#### `number`
