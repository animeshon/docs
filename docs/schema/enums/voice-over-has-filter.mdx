---
id: voice-over-has-filter
title: VoiceOverHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum VoiceOverHasFilter {
  voices
  id
  selfLink
  createdAt
  modifiedAt
  version
  names
  aliases
  description
}
```

### Values

#### `voices`

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `names`

#### `aliases`

#### `description`
