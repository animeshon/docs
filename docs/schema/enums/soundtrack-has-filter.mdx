---
id: soundtrack-has-filter
title: SoundtrackHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum SoundtrackHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  content
  song
  soundTrackType
  trackVersion
  localization
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `content`

#### `song`

#### `soundTrackType`

#### `trackVersion`

#### `localization`
