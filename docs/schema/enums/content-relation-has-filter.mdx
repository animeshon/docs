---
id: content-relation-has-filter
title: ContentRelationHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ContentRelationHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  subject
  object
  type
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `subject`

#### `object`

#### `type`
