---
id: sound-track-type
title: SoundTrackType
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum SoundTrackType {
  UNKNOWN
  BACKGROUND
  ENDING
  IMAGE
  INSERT
  OPENING
  THEME
}
```

### Values

#### `UNKNOWN`

#### `BACKGROUND`

#### `ENDING`

#### `IMAGE`

#### `INSERT`

#### `OPENING`

#### `THEME`
