---
id: genre-has-filter
title: GenreHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum GenreHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  names
  aliases
  description
  entities
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `names`

#### `aliases`

#### `description`

#### `entities`
