---
id: content-has-filter
title: ContentHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ContentHasFilter {
  status
  relations
}
```

### Values

#### `status`

#### `relations`
