---
id: host-has-filter
title: HostHasFilter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum HostHasFilter {
  id
  selfLink
  createdAt
  modifiedAt
  version
  IP
  eTLD
  domain
  subdomains
}
```

### Values

#### `id`

#### `selfLink`

#### `createdAt`

#### `modifiedAt`

#### `version`

#### `IP`

#### `eTLD`

#### `domain`

#### `subdomains`
