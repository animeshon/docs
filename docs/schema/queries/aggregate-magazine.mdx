---
id: aggregate-magazine
title: aggregateMagazine
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateMagazine(
  filter: MagazineFilter
): MagazineAggregateResult

```

### Arguments

#### `filter` ([`MagazineFilter`](/docs/schema/inputs/magazine-filter))

### Type

#### [`MagazineAggregateResult`](/docs/schema/objects/magazine-aggregate-result)
