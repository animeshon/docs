---
id: get-cross-reference
title: getCrossReference
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
getCrossReference(
  id: String!
): CrossReference

```

### Arguments

#### `id` ([`String!`](/docs/schema/scalars/string))

### Type

#### [`CrossReference`](/docs/schema/objects/cross-reference)
