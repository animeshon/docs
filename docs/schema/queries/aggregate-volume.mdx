---
id: aggregate-volume
title: aggregateVolume
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateVolume(
  filter: VolumeFilter
): VolumeAggregateResult

```

### Arguments

#### `filter` ([`VolumeFilter`](/docs/schema/inputs/volume-filter))

### Type

#### [`VolumeAggregateResult`](/docs/schema/objects/volume-aggregate-result)
