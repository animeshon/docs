---
id: get-character
title: getCharacter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
getCharacter(
  id: String!
): Character

```

### Arguments

#### `id` ([`String!`](/docs/schema/scalars/string))

### Type

#### [`Character`](/docs/schema/objects/character)
