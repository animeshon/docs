---
id: get-song
title: getSong
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
getSong(
  id: String!
): Song

```

### Arguments

#### `id` ([`String!`](/docs/schema/scalars/string))

### Type

#### [`Song`](/docs/schema/objects/song)
