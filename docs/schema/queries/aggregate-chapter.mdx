---
id: aggregate-chapter
title: aggregateChapter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateChapter(
  filter: ChapterFilter
): ChapterAggregateResult

```

### Arguments

#### `filter` ([`ChapterFilter`](/docs/schema/inputs/chapter-filter))

### Type

#### [`ChapterAggregateResult`](/docs/schema/objects/chapter-aggregate-result)
