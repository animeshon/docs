---
id: query-manga
title: queryManga
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
queryManga(
  filter: MangaFilter
  order: MangaOrder
  first: QueryPaginationInt!
  offset: Int = 0
): [Manga]

```

### Arguments

#### `filter` ([`MangaFilter`](/docs/schema/inputs/manga-filter))

#### `order` ([`MangaOrder`](/docs/schema/inputs/manga-order))

#### `first` ([`QueryPaginationInt!`](/docs/schema/scalars/query-pagination-int))

#### `offset` ([`Int`](/docs/schema/scalars/int))

### Type

#### [`Manga`](/docs/schema/objects/manga)
