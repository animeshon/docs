---
id: aggregate-free-text-role
title: aggregateFreeTextRole
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateFreeTextRole(
  filter: FreeTextRoleFilter
): FreeTextRoleAggregateResult

```

### Arguments

#### `filter` ([`FreeTextRoleFilter`](/docs/schema/inputs/free-text-role-filter))

### Type

#### [`FreeTextRoleAggregateResult`](/docs/schema/objects/free-text-role-aggregate-result)
