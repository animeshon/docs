---
id: aggregate-music-collection
title: aggregateMusicCollection
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateMusicCollection(
  filter: MusicCollectionFilter
): MusicCollectionAggregateResult

```

### Arguments

#### `filter` ([`MusicCollectionFilter`](/docs/schema/inputs/music-collection-filter))

### Type

#### [`MusicCollectionAggregateResult`](/docs/schema/objects/music-collection-aggregate-result)
