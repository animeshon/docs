---
id: aggregate-convention
title: aggregateConvention
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateConvention(
  filter: ConventionFilter
): ConventionAggregateResult

```

### Arguments

#### `filter` ([`ConventionFilter`](/docs/schema/inputs/convention-filter))

### Type

#### [`ConventionAggregateResult`](/docs/schema/objects/convention-aggregate-result)
