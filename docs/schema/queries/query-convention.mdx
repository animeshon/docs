---
id: query-convention
title: queryConvention
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
queryConvention(
  filter: ConventionFilter
  order: ConventionOrder
  first: QueryPaginationInt!
  offset: Int = 0
): [Convention]

```

### Arguments

#### `filter` ([`ConventionFilter`](/docs/schema/inputs/convention-filter))

#### `order` ([`ConventionOrder`](/docs/schema/inputs/convention-order))

#### `first` ([`QueryPaginationInt!`](/docs/schema/scalars/query-pagination-int))

#### `offset` ([`Int`](/docs/schema/scalars/int))

### Type

#### [`Convention`](/docs/schema/objects/convention)
