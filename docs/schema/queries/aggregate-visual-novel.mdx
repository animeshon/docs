---
id: aggregate-visual-novel
title: aggregateVisualNovel
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateVisualNovel(
  filter: VisualNovelFilter
): VisualNovelAggregateResult

```

### Arguments

#### `filter` ([`VisualNovelFilter`](/docs/schema/inputs/visual-novel-filter))

### Type

#### [`VisualNovelAggregateResult`](/docs/schema/objects/visual-novel-aggregate-result)
