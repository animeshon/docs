---
id: query-metadata
title: queryMetadata
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
queryMetadata(
  filter: MetadataFilter
  order: MetadataOrder
  first: QueryPaginationInt!
  offset: Int = 0
): [Metadata]

```

### Arguments

#### `filter` ([`MetadataFilter`](/docs/schema/inputs/metadata-filter))

#### `order` ([`MetadataOrder`](/docs/schema/inputs/metadata-order))

#### `first` ([`QueryPaginationInt!`](/docs/schema/scalars/query-pagination-int))

#### `offset` ([`Int`](/docs/schema/scalars/int))

### Type

#### [`Metadata`](/docs/schema/interfaces/metadata)
