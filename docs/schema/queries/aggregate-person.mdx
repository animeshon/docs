---
id: aggregate-person
title: aggregatePerson
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregatePerson(
  filter: PersonFilter
): PersonAggregateResult

```

### Arguments

#### `filter` ([`PersonFilter`](/docs/schema/inputs/person-filter))

### Type

#### [`PersonAggregateResult`](/docs/schema/objects/person-aggregate-result)
