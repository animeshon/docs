---
id: get-voice-over
title: getVoiceOver
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
getVoiceOver(
  id: String!
): VoiceOver

```

### Arguments

#### `id` ([`String!`](/docs/schema/scalars/string))

### Type

#### [`VoiceOver`](/docs/schema/objects/voice-over)

VoiceOver is any voice that is not part of a character (e.g. inner voice, narrator, etc.)
