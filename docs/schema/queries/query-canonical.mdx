---
id: query-canonical
title: queryCanonical
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
queryCanonical(
  filter: CanonicalFilter
  order: CanonicalOrder
  first: QueryPaginationInt!
  offset: Int = 0
): [Canonical]

```

### Arguments

#### `filter` ([`CanonicalFilter`](/docs/schema/inputs/canonical-filter))

#### `order` ([`CanonicalOrder`](/docs/schema/inputs/canonical-order))

#### `first` ([`QueryPaginationInt!`](/docs/schema/scalars/query-pagination-int))

#### `offset` ([`Int`](/docs/schema/scalars/int))

### Type

#### [`Canonical`](/docs/schema/objects/canonical)
