---
id: aggregate-anime
title: aggregateAnime
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateAnime(
  filter: AnimeFilter
): AnimeAggregateResult

```

### Arguments

#### `filter` ([`AnimeFilter`](/docs/schema/inputs/anime-filter))

### Type

#### [`AnimeAggregateResult`](/docs/schema/objects/anime-aggregate-result)
