---
id: aggregate-universe
title: aggregateUniverse
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateUniverse(
  filter: UniverseFilter
): UniverseAggregateResult

```

### Arguments

#### `filter` ([`UniverseFilter`](/docs/schema/inputs/universe-filter))

### Type

#### [`UniverseAggregateResult`](/docs/schema/objects/universe-aggregate-result)
