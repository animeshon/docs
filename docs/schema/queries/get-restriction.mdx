---
id: get-restriction
title: getRestriction
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
getRestriction(
  id: String!
): Restriction

```

### Arguments

#### `id` ([`String!`](/docs/schema/scalars/string))

### Type

#### [`Restriction`](/docs/schema/objects/restriction)
