---
id: get-free-text-role
title: getFreeTextRole
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
getFreeTextRole(
  id: String!
): FreeTextRole

```

### Arguments

#### `id` ([`String!`](/docs/schema/scalars/string))

### Type

#### [`FreeTextRole`](/docs/schema/objects/free-text-role)
