---
id: get-doujinshi
title: getDoujinshi
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
getDoujinshi(
  id: String!
): Doujinshi

```

### Arguments

#### `id` ([`String!`](/docs/schema/scalars/string))

### Type

#### [`Doujinshi`](/docs/schema/objects/doujinshi)
