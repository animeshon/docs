---
id: aggregate-character
title: aggregateCharacter
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateCharacter(
  filter: CharacterFilter
): CharacterAggregateResult

```

### Arguments

#### `filter` ([`CharacterFilter`](/docs/schema/inputs/character-filter))

### Type

#### [`CharacterAggregateResult`](/docs/schema/objects/character-aggregate-result)
