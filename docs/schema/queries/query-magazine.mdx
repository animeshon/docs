---
id: query-magazine
title: queryMagazine
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
queryMagazine(
  filter: MagazineFilter
  order: MagazineOrder
  first: QueryPaginationInt!
  offset: Int = 0
): [Magazine]

```

### Arguments

#### `filter` ([`MagazineFilter`](/docs/schema/inputs/magazine-filter))

#### `order` ([`MagazineOrder`](/docs/schema/inputs/magazine-order))

#### `first` ([`QueryPaginationInt!`](/docs/schema/scalars/query-pagination-int))

#### `offset` ([`Int`](/docs/schema/scalars/int))

### Type

#### [`Magazine`](/docs/schema/objects/magazine)
