---
id: aggregate-song
title: aggregateSong
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateSong(
  filter: SongFilter
): SongAggregateResult

```

### Arguments

#### `filter` ([`SongFilter`](/docs/schema/inputs/song-filter))

### Type

#### [`SongAggregateResult`](/docs/schema/objects/song-aggregate-result)
