---
id: aggregate-light-novel
title: aggregateLightNovel
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateLightNovel(
  filter: LightNovelFilter
): LightNovelAggregateResult

```

### Arguments

#### `filter` ([`LightNovelFilter`](/docs/schema/inputs/light-novel-filter))

### Type

#### [`LightNovelAggregateResult`](/docs/schema/objects/light-novel-aggregate-result)
