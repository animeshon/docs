---
id: get-canonical
title: getCanonical
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
getCanonical(
  id: String!
): Canonical

```

### Arguments

#### `id` ([`String!`](/docs/schema/scalars/string))

### Type

#### [`Canonical`](/docs/schema/objects/canonical)
