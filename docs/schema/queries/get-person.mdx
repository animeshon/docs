---
id: get-person
title: getPerson
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
getPerson(
  id: String!
): Person

```

### Arguments

#### `id` ([`String!`](/docs/schema/scalars/string))

### Type

#### [`Person`](/docs/schema/objects/person)
