---
id: query-relation-label
title: queryRelationLabel
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
queryRelationLabel(
  filter: RelationLabelFilter
  order: RelationLabelOrder
  first: QueryPaginationInt!
  offset: Int = 0
): [RelationLabel]

```

### Arguments

#### `filter` ([`RelationLabelFilter`](/docs/schema/inputs/relation-label-filter))

#### `order` ([`RelationLabelOrder`](/docs/schema/inputs/relation-label-order))

#### `first` ([`QueryPaginationInt!`](/docs/schema/scalars/query-pagination-int))

#### `offset` ([`Int`](/docs/schema/scalars/int))

### Type

#### [`RelationLabel`](/docs/schema/objects/relation-label)
