---
id: query-person
title: queryPerson
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
queryPerson(
  filter: PersonFilter
  order: PersonOrder
  first: QueryPaginationInt!
  offset: Int = 0
): [Person]

```

### Arguments

#### `filter` ([`PersonFilter`](/docs/schema/inputs/person-filter))

#### `order` ([`PersonOrder`](/docs/schema/inputs/person-order))

#### `first` ([`QueryPaginationInt!`](/docs/schema/scalars/query-pagination-int))

#### `offset` ([`Int`](/docs/schema/scalars/int))

### Type

#### [`Person`](/docs/schema/objects/person)
