---
id: get-age-rating
title: getAgeRating
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
getAgeRating(
  id: String!
): AgeRating

```

### Arguments

#### `id` ([`String!`](/docs/schema/scalars/string))

### Type

#### [`AgeRating`](/docs/schema/objects/age-rating)
