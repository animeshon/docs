---
id: aggregate-doujinshi
title: aggregateDoujinshi
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateDoujinshi(
  filter: DoujinshiFilter
): DoujinshiAggregateResult

```

### Arguments

#### `filter` ([`DoujinshiFilter`](/docs/schema/inputs/doujinshi-filter))

### Type

#### [`DoujinshiAggregateResult`](/docs/schema/objects/doujinshi-aggregate-result)
