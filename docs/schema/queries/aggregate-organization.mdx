---
id: aggregate-organization
title: aggregateOrganization
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
aggregateOrganization(
  filter: OrganizationFilter
): OrganizationAggregateResult

```

### Arguments

#### `filter` ([`OrganizationFilter`](/docs/schema/inputs/organization-filter))

### Type

#### [`OrganizationAggregateResult`](/docs/schema/objects/organization-aggregate-result)
