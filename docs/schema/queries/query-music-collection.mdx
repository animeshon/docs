---
id: query-music-collection
title: queryMusicCollection
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
queryMusicCollection(
  filter: MusicCollectionFilter
  order: MusicCollectionOrder
  first: QueryPaginationInt!
  offset: Int = 0
): [MusicCollection]

```

### Arguments

#### `filter` ([`MusicCollectionFilter`](/docs/schema/inputs/music-collection-filter))

#### `order` ([`MusicCollectionOrder`](/docs/schema/inputs/music-collection-order))

#### `first` ([`QueryPaginationInt!`](/docs/schema/scalars/query-pagination-int))

#### `offset` ([`Int`](/docs/schema/scalars/int))

### Type

#### [`MusicCollection`](/docs/schema/objects/music-collection)
