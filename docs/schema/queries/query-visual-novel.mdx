---
id: query-visual-novel
title: queryVisualNovel
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
queryVisualNovel(
  filter: VisualNovelFilter
  order: VisualNovelOrder
  first: QueryPaginationInt!
  offset: Int = 0
): [VisualNovel]

```

### Arguments

#### `filter` ([`VisualNovelFilter`](/docs/schema/inputs/visual-novel-filter))

#### `order` ([`VisualNovelOrder`](/docs/schema/inputs/visual-novel-order))

#### `first` ([`QueryPaginationInt!`](/docs/schema/scalars/query-pagination-int))

#### `offset` ([`Int`](/docs/schema/scalars/int))

### Type

#### [`VisualNovel`](/docs/schema/objects/visual-novel)
